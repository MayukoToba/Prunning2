<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owner.prunning">

    <application
        android:name="com.activeandroid.app.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="AA_DB_NAME"
            android:value="yotei.db"/>
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EnterActivity" />
        <activity android:name=".DisplayActivity" />
        <activity android:name=".SetteiActivity" />
        <activity android:name=".SubjectSettingActivity" />
        <activity android:name=".NaiyouSettingActivity" />
        <activity android:name=".TestDateActivity" />
        <activity android:name=".TuuthiTimeSettingActivity"></activity>
    </application>

</manifest>